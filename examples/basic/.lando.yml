name: mailpit-basic
recipe: lamp
config:
  php: 8.4

services:
  smtpserver:
    type: mailpit

# This is important because it lets lando know to test against the plugin in this repo
# DO NOT REMOVE THIS!
plugins:
  "@lando/mailpit": ../..
